describe('job service', () => {
  it('should construct', () => {
    console.log(1);
    console.log(1);
    console.log(1);
    console.log(1);
  });
  it('should create a job', () => {});
  it('should list jobs', () => {});
  it('should delete jobs', () => {});
});
