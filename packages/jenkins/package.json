{
  "name": "@anchorage-deploy/jenkins",
  "version": "1.0.0",
  "description": "Connects to jenkins n shiz",
  "main": "index.ts",
  "scripts": {
    "start": "npm run compile && node ./out/index.js",
    "debug": "node --inspect-brk ./out/index.js",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "author": "Ur Mum",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "jest": "^25.4.0",
    "typescript": "~3.8.0",
    "gts": "^2.0.0",
    "@types/node": "^10.0.3"
  },
  "dependencies": {
    "jenkins": "^0.28.0"
  }
}
